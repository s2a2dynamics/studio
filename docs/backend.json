{
  "entities": {
    "Contact": {
      "title": "Contact",
      "description": "Represents a single contact, identified by their WhatsApp number.",
      "type": "object",
      "properties": {
        "whatsappNumber": {
          "type": "string",
          "description": "The user's unique WhatsApp number."
        },
        "message": {
          "type": "string",
          "description": "The last message content submitted by the user."
        },
        "lastMessageAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the last message was submitted."
        },
        "from": {
          "type": "string",
          "enum": ["user", "ai"],
          "description": "Indicates the origin of the last message."
        }
      },
      "required": ["whatsappNumber", "message", "lastMessageAt", "from"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/contacts/{contactId}": {
      "schema": {
        "$ref": "#/backend/entities/Contact"
      },
      "description": "Stores unique contacts who have sent messages."
    }
  }
}
