{
  "entities": {
    "Contact": {
      "title": "Contact",
      "description": "Represents a single message submission from a user.",
      "type": "object",
      "properties": {
        "whatsappNumber": {
          "type": "string",
          "description": "The user's WhatsApp number."
        },
        "message": {
          "type": "string",
          "description": "The message content submitted by the user."
        },
        "lastMessageAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the message was submitted."
        },
        "from": {
          "type": "string",
          "enum": ["user", "ai"],
          "description": "Indicates the origin of the message, always 'user' for form submissions."
        }
      },
      "required": ["whatsappNumber", "message", "lastMessageAt", "from"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/contacts/{contactId}": {
      "schema": {
        "$ref": "#/backend/entities/Contact"
      },
      "description": "Stores individual messages sent via the contact form."
    }
  }
}
